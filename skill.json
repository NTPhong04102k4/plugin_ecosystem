{
  "version": "1",
  "project": "my-plugin-ecosystem",

  // Base rules apply everywhere. Stack-specific content (architecture, conventions,
  // lint, templates, design-system, library-docs) is NOT here — it lives in
  // packs/<stack>.json and is merged in at emit time. That separation is what lets
  // one skill definition serve every stack without duplication.
  "rules": {
    "working-policy": [
      "When the request is ambiguous, ask via AskUserQuestion before coding — do not guess.",
      "Write a short plan / work-log before any code change; get approval for non-trivial work.",
      "Minimal change only: stay within the requested scope, no drive-by refactors.",
      "Verify with `git status` / `git diff` before finishing; report what changed.",
      "Never commit on your own — present changes and let the user decide."
    ],
    "technical": [
      "Generated output must be deterministic: same input => same output.",
      "No new dependency without justification.",
      "Never overwrite a file the tool did not generate without explicit approval."
    ],
    "commit-format": [
      "Message format: `ref-<jira-number>: <type> - <short description>` (e.g. `ref-1433: chore - add make help target`).",
      "Use standard Conventional Commits types only: feat, fix, docs, style, refactor, perf, test, build, ci, chore, revert.",
      "The `ref-<number>` is the Jira ticket for task tracking; ask the user for it (or derive it from the branch name) if unknown.",
      "Subject line MUST NOT exceed 150 characters (hard limit); keep it concise — do not pad it.",
      "Description is imperative; add an optional body for details, separated by a blank line.",
      "Do NOT add a Co-Authored-By or any author trailer."
    ],
    "module-registry": [
      "Each project keeps a module registry at `docs/module-registry.md`.",
      "For every module, record: Purpose (what it develops), Files (key files + their role), Routes (route path -> screen/handler).",
      "After delivering a feature, update the registry so future work can look up which files/routes belong to which module.",
      "Keep entries stable and sorted; do not rewrite unrelated sections."
    ],
    "project-profile": [
      "The project's learned knowledge lives in `docs/project-profile.md`: framework + language (with versions), architecture/layers and how they connect, key flows (data/auth/navigation), important config/variables/tokens, and a catalog of reusable components/widgets WITH file paths.",
      "FIRST task in a project (profile missing): run `learn-project` to build it before implementing. Check with `skillrunner status`.",
      "If the profile EXISTS: reuse it — apply the cataloged components/patterns directly; do NOT re-scan the whole source to rediscover them.",
      "Rebuild the profile ONLY when the user explicitly asks to update it (re-run `learn-project`); never auto-refresh.",
      "Safety: trust the profile for orientation, but cheaply confirm a specific file/symbol still exists before relying on it."
    ]
  },

  // Rule groups supplied by a stack pack. Skills may reference these in
  // appliesRules; they resolve once `skillrunner` merges the detected pack.
  "packRules": ["architecture", "conventions", "lint", "templates", "design-system", "library-docs"],

  "skills": {

    // ---- Core skills: generic, identical across every stack ----

    "plan-feature": {
      "description": "Turn a feature request into a plan + goals for the user to decide.",
      "goal": "Produce a reviewable plan; do NOT write code yet.",
      "appliesRules": ["working-policy"],
      "requiresApproval": true,
      "inputs": ["The feature request", "Relevant existing files/structure"],
      "instructions": [
        "Restate the request in one sentence to confirm understanding.",
        "List 2-4 measurable candidate goals for the user to pick from.",
        "Draft a step-by-step plan naming the files that would change.",
        "Call out risks, unknowns, and decisions only the user can make.",
        "Present everything and STOP for the user's decision."
      ],
      "outputs": ["A plan document — no code changes"]
    },

    "spec-from-source": {
      "description": "Turn a Jira/Confluence issue or a Swagger/OpenAPI file into a structured spec.",
      "goal": "A clear spec (requirements, acceptance criteria, models, endpoints, flow) before coding.",
      "appliesRules": ["working-policy"],
      "requiresApproval": true,
      "inputs": ["A Jira/Confluence link, or an OpenAPI/Swagger file path"],
      "instructions": [
        "Read the source (via Atlassian MCP for Jira/Confluence, or parse the OpenAPI file).",
        "Extract: requirements, acceptance criteria, data models, endpoints, UI flow.",
        "List ambiguities and open questions explicitly.",
        "Write the spec in the user's language; do not start implementation.",
        "Present and STOP for confirmation before any code or write-back."
      ],
      "outputs": ["A structured spec document — no code changes"]
    },

    "commit": {
      "description": "Split the working changes into per-module commits with Conventional Commit messages tagged by a Jira ref.",
      "goal": "Reviewable, module-scoped commits in the `ref-<n>: <type> - ...` format — only after the user confirms.",
      "appliesRules": ["working-policy", "commit-format"],
      "inputs": ["The Jira ticket number(s) for the work (ask if unknown)"],
      "instructions": [
        "Run `git status` and `git diff` to understand what changed.",
        "Determine the Jira ref: derive it from the branch name if possible, otherwise ask the user.",
        "Group changes by module/feature; propose one commit per group.",
        "Draft each message as `ref-<jira-number>: <type> - <short description>` (<=150 chars) using a valid Conventional Commit type.",
        "Present ONLY the proposed commit message text(s) — do NOT dump file diffs. STOP for the user's confirmation.",
        "Commit only after the user confirms. Do NOT push — the user pushes themselves.",
        "Do not append any author/Co-Authored-By trailer."
      ],
      "outputs": ["The proposed commit message text(s) for confirmation, then the commits (not pushed)"]
    },

    "refactor": {
      "description": "Improve a component/module for clarity and reuse WITHOUT changing behavior.",
      "goal": "Cleaner code, identical behavior, minimal diff.",
      "appliesRules": ["working-policy", "conventions", "lint"],
      "inputs": ["The target file(s)"],
      "instructions": [
        "Read the target and identify duplication, dead code, deep nesting.",
        "Apply early-returns, extract reused pieces, prefer existing shared components.",
        "Do NOT change observable behavior or public signatures without approval.",
        "Run the stack linter and report the before/after diff."
      ],
      "outputs": ["Refactored files", "Lint clean", "A diff summary"]
    },

    "explain-lib": {
      "description": "Explain how THIS project actually uses a given library (no invented APIs).",
      "goal": "Accurate what/where/pattern/pitfalls, grounded in real usage.",
      "appliesRules": ["working-policy", "library-docs"],
      "inputs": ["The library name"],
      "instructions": [
        "Read the project's curated library docs (library-docs pack rules) first.",
        "Find real call sites in the codebase to confirm usage.",
        "Summarize: what it's used for, where, the standard pattern, common pitfalls.",
        "Do not invent APIs — if unknown, say so."
      ],
      "outputs": ["A grounded explanation"]
    },

    "check-diff": {
      "description": "Review the current git diff against the stack's conventions.",
      "goal": "Report convention violations with file:line, then run the linter.",
      "appliesRules": ["working-policy", "conventions", "lint"],
      "instructions": [
        "Run `git diff` to get the pending changes.",
        "Check each change against the conventions rules for this stack.",
        "Report violations as `file:line — rule` with a suggested fix.",
        "Run the stack linter and include its results."
      ],
      "outputs": ["A violation report", "Linter results"]
    },

    "learn-project": {
      "description": "Deep-dive a project ONCE and cache its architecture in docs/project-profile.md (reused later, refreshed only on explicit update).",
      "goal": "A reusable profile: framework, language, architecture, flows, config, and a catalog of reusable components with paths.",
      "appliesRules": ["working-policy", "project-profile", "architecture", "conventions", "design-system", "library-docs"],
      "inputs": ["The project directory (stack is auto-detected)"],
      "instructions": [
        "Run `skillrunner status`: if docs/project-profile.md already exists, STOP unless the user explicitly asked to update it.",
        "Detect the stack and load the matching pack rules for context.",
        "Analyze the project: framework + language (with versions), architecture/layers and how they connect, key flows (data/auth/navigation), important config/env/variables/design tokens.",
        "Catalog reusable components/widgets: name, file path, purpose, and how to use — so future tasks apply them without re-reading source.",
        "Write docs/project-profile.md in a stable, structured format. Do NOT modify source code.",
        "Summarize what was learned."
      ],
      "outputs": ["docs/project-profile.md", "A short summary of the project's architecture"]
    },

    "update-module-registry": {
      "description": "Create or update the project's module registry (docs/module-registry.md).",
      "goal": "Keep an accurate map of module -> purpose, files, and routes for future lookup.",
      "appliesRules": ["working-policy", "module-registry"],
      "inputs": ["The module that was touched and the files/routes added or changed"],
      "instructions": [
        "Read docs/module-registry.md (create it if missing).",
        "Find or add the section for the touched module.",
        "Record Purpose (what it develops), Files (key files + role), Routes (path -> screen/handler).",
        "Keep entries sorted and stable; leave unrelated sections untouched.",
        "Report which module sections changed."
      ],
      "outputs": ["Updated docs/module-registry.md", "A summary of what changed"]
    },

    // ---- Orchestration: the end-to-end feature delivery workflow ----

    "deliver-feature": {
      "description": "Deliver a Jira feature end to end: locate module -> plan -> goal -> code -> check -> clean -> verify -> commit.",
      "goal": "A verified, convention-clean feature with the module registry updated and a confirmed commit (not pushed).",
      "appliesRules": ["working-policy", "project-profile", "module-registry", "commit-format", "architecture", "conventions", "design-system", "lint"],
      "inputs": ["The Jira task code", "The problem description"],
      "instructions": [
        "If the Jira code or problem description is missing, ask for it.",
        "Run `skillrunner status`. If docs/project-profile.md is missing, run `learn-project` first; otherwise reuse it — do not re-scan the whole source.",
        "Consult CLAUDE.md, docs/project-profile.md, and docs/module-registry.md to locate the relevant module, its files, routes, and reusable components.",
        "Produce a plan and 2-4 measurable goals; present them and STOP for the user's approval before coding.",
        "Implement the code/components per the stack conventions and design system; reuse existing components first.",
        "Run check-diff against conventions and run the linter; clean/refactor the touched components without changing behavior.",
        "Update docs/module-registry.md for the module (purpose / files / routes).",
        "Verify the change end to end (drive the affected flow, not just build/lint).",
        "Propose the commit message(s) as `ref-<jira>: <type> - <desc>` (<=150 chars) TEXT ONLY — no file dumps — and STOP for confirmation.",
        "Commit only after the user confirms. Do NOT push — the user pushes themselves."
      ],
      "outputs": ["Implemented + verified feature", "Updated module registry", "A confirmed commit (not pushed)"]
    },

    // ---- Stack-parametrized skills: one intent, content from the pack ----

    "scaffold-data": {
      "description": "Build the data layer for an API call following the stack's architecture.",
      "goal": "Types/models + service/provider + wiring, per the pack's templates.",
      "appliesRules": ["working-policy", "project-profile", "technical", "architecture", "conventions", "templates", "library-docs"],
      "inputs": ["Endpoint, method, request/response shape (ask if unknown)"],
      "instructions": [
        "Confirm the endpoint contract; ask for missing request/response shapes.",
        "Copy the pack's data-layer template (do not invent a new structure).",
        "Generate the model/types (null-defensive) and the service/provider.",
        "Wire it into the repository/hook layer as the architecture requires.",
        "Do NOT build UI here; stop at the data layer."
      ],
      "outputs": ["Data-layer files following the pack conventions"]
    },

    "scaffold-screen": {
      "description": "Scaffold a new screen/feature following the stack's architecture.",
      "goal": "A screen split into the pack's layers, wired into routing.",
      "appliesRules": ["working-policy", "project-profile", "architecture", "conventions", "templates"],
      "inputs": ["Screen name, purpose, whether it is a list/detail/form"],
      "instructions": [
        "Pick the matching pack template (list / detail / form / dialog).",
        "Generate the layers the architecture requires (view / logic / data split).",
        "Register the route per the routing convention.",
        "Follow all naming and structure conventions from the pack."
      ],
      "outputs": ["Screen files + route registration"]
    },

    "build-ui": {
      "description": "Build or edit UI using the stack's design system and tokens.",
      "goal": "UI that reuses shared components and obeys design tokens.",
      "appliesRules": ["working-policy", "project-profile", "conventions", "design-system"],
      "inputs": ["The UI to build/change and where it lives"],
      "instructions": [
        "Reuse existing shared components before creating new ones.",
        "Use semantic design tokens — never hardcode colors/spacing.",
        "Follow the design-system pack patterns for the component type.",
        "Keep behavior and accessibility feedback (loading/success/error) visible."
      ],
      "outputs": ["UI files using shared components and tokens"]
    },

    "gen-routes": {
      "description": "Generate/refresh route entries from the project's file structure.",
      "goal": "Deterministic file -> route mapping following the convention.",
      "appliesRules": ["working-policy", "architecture", "conventions"],
      "inputs": ["The routing convention and the pages/routes directory"],
      "instructions": [
        "Scan the configured directory for route files.",
        "Derive each route path from the convention.",
        "Write the routes file with stable, sorted output.",
        "Report added/removed/changed routes as a diff summary."
      ],
      "outputs": ["The generated routes file", "A diff summary"]
    }
  }
}
